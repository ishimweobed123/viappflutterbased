import 'package:flutter/material.dart';

class UserDangerZonesScreen extends StatelessWidget {
  const UserDangerZonesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Danger Zones'),
      ),
      body: const Center(
        child: Text('Danger Zones screen coming soon.'),
      ),
    );
  }
}
